variables:
  DH_REPOSITORY: zammad

before_script:
  - docker info

github:
  script:
    - build/sync_repo.sh git@github.com:zammad/docker.git

build_image:
  only:
    - master
    - develop
    - tags
  script:
    - build/build_image.sh
