variables:
  DH_REPOSITORY: zammad

before_script:
  - docker info

github:
  script:
    - scripts/sync_repo.sh git@github.com:zammad/docker.git

build_image:
  only:
    - master
    - develop
    - tags
  script:
    - scripts/build_image.sh
